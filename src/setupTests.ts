/* `Jest-dom` adds custom jest matches for asserting on DOM nodes.
 It allows you to do things like: `expect(element).toHaveTextContent(/react/i)`.
 You can learn more here: "https://github.com/testing-library/jest-dom" */
import '@testing-library/jest-dom';
